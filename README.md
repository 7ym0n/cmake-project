##Test Using CMAKE Build Project
### learn cmake build c or c++ source code.

```shell
	mkdir -p build
	cd build
	cmake -DCMAKE_INSTALL_PREFIX=${YOUR_INSTALL_PREFIX} ..
	make
	make install
```